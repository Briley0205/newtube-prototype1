extends base.pug

block content 
    form(method="POST", action="/videos/upload")
        input(name="video title" placeholder="title", required, type="text")
        input(type="submit", value="Upload Video")